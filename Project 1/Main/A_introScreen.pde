void introScreen() {
  background(purple);
  textSize(102);
  fill(white);
  text("ARENA", width/2, height/2);
  
  textSize(100);
  fill(black);
  text("ARENA", width/2, height/2);
}

void introClicks() {
  mode = 2;
}
